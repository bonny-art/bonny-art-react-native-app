import { mscale } from "@shared/lib/responsive";
import { sizes } from "@/shared/lib/tokens";

export const MODAL_WIDTH_PERCENTAGE = "82%";
export const MODAL_MAX_WIDTH = mscale(sizes.modal.maxWidth);
